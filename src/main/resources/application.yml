spring:
  output:
    ansi:
      enabled: always

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  h2:
    console:
      enabled: true

logging.level:
  org.hibernate.SQL: debug